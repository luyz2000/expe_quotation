%h1 Edit Quotation Item

.card
  .card-body
    = render 'form'

.card-footer
  = link_to "Cancel", quotation_path(@quotation), class: "btn btn-secondary"
  = link_to "Delete", [@quotation, @quotation_item], method: :delete, class: "btn btn-danger", confirm: "Are you sure?"